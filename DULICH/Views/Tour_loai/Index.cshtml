@model IEnumerable<DULICH.Models.Tour_loai>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.loai_ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.loai_mota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.loai_ten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.loai_mota)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.loai_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.loai_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.loai_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
