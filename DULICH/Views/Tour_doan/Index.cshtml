@model IEnumerable<DULICH.Models.Tour_doan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tour_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.gia_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.doan_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.doan_ngaydi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.doan_ngayve)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.doan_chitietchuongtrinh)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tour_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.gia_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.doan_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.doan_ngaydi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.doan_ngayve)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.doan_chitietchuongtrinh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.doan_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.doan_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.doan_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
