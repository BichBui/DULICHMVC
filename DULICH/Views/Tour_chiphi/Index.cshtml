@model IEnumerable<DULICH.Models.Tour_chiphi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.doan_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.chiphi_total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.chiphi_chitiet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.doan_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.chiphi_total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.chiphi_chitiet)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.chiphi_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.chiphi_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.chiphi_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
